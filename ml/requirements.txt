# NutriFit-AI - machine learning environment
#
# scikit-learn is PINNED to the version Google Colab used for training
# (see ml/artifacts/model_card.json -> environment). A pipeline pickled by
# 1.6.1 will not load under 1.9.x, so the local environment must match Colab's
# or `export_models.py` will fail its reload check.
#
# If you retrain on a newer Colab image, update this pin AND
# services/ml-service/requirements.txt to the version in the new model card.
scikit-learn==1.6.1
numpy>=2.0,<2.3
pandas>=2.2,<3
joblib>=1.3
scipy>=1.11

# Visualisation (EDA / evaluation notebooks and report figures)
matplotlib>=3.8
seaborn>=0.13

# Notebook tooling
jupyter>=1.0
ipykernel>=6.29

# Testing
pytest>=8.0
pytest-cov>=5.0
