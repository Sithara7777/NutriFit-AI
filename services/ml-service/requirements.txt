# NutriFit-AI ML microservice
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# PINNED to the versions the models were trained with in Google Colab.
# See ml/artifacts/model_card.json -> environment.
#
# These are exact pins, not ranges, and that is deliberate. A scikit-learn
# pipeline pickle embeds references to private classes; loading a model trained
# on 1.6.1 under 1.9.0 fails with:
#
#   Can't get attribute '_RemainderColsList' on sklearn.compose._column_transformer
#
# The serving environment must therefore match the training environment. If you
# retrain on a newer scikit-learn, update BOTH this file and ml/requirements.txt
# to the version recorded in the new model card.
scikit-learn==1.6.1
numpy>=2.0,<2.3
pandas>=2.2,<3
joblib>=1.3
scipy>=1.11

# Testing
pytest>=8.0
httpx>=0.27
